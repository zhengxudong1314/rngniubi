<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="MyAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>
    <!--沉浸式主题-->
    <style name="ImmersiveTheme" parent="MyAppTheme"/>

    <style name="AppDialog" parent="AlertDialog.AppCompat">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 设置背景色 透明-->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 设置是否显示背景 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 设置背景透明度 -->
        <item name="android:backgroundDimAmount">0.5</item>
    </style>
    <style name="AppDialogRadius" parent="AppDialog">
        <!--控制dialog圆角-->
        <item name="android:windowBackground">@drawable/shape_white_radius</item>
    </style>
    <style name="AppDialogBottom" parent="AppDialog">
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:windowAnimationStyle">@style/dialog_animation_bottom</item>
    </style>

    <!-- 底部dialog进出动画 -->
    <style name="dialog_animation_bottom">
        <item name="android:windowEnterAnimation">@anim/dialog_anim_in_bottom</item>
        <item name="android:windowExitAnimation">@anim/dialog_anim_out_bottom</item>
    </style>
</resources>
